// Diff Tab webview entry point (scaffold). Input layout, splitters, the
// jsdiff-backed alignment model, and rendering all land in later steps.
(function main() {
  // Intentionally empty for now — bundled by esbuild into media/main.js.
})();
